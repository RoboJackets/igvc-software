This is the power / estop schematic for Jeanni.

Basic idea is the first relay is self latching, and latches if both e-stops are enabled and the green button is pressed. It will then energise the main relay, which provides power to everything else.

Need to open gschem in this dir for it to work.
